
n miniMaxSum(arr) {
	    // Write your code here
	//     // find the min value of the array and the max value of the array and then exclude the minimum
	//         let minimum = arr[0]; let maximum = 0; let sum = 0;
	//             arr.forEach(element => {
	//                     if (element < minimum){minimum = element};
	//                             if (element > maximum){maximum = element};
	//                                     sum += element;
	//                                         })
	//                                             const maxSum = sum - minimum; 
	//                                                 const minSum = sum - maximum;
	//                                                     console.log(minSum, maxSum)
	//                                                     }
